{
  "postgresql": {
    "db_superuser_password": "${postgresql_superuser_password}",
    "replication_password": "${postgresql_replication_password}"
  },
  "etcd": {
    "initial_cluster_token": "${etcd_initial_cluster_token}"
  },
  "elasticsearch": {
    "cluster_name": "${elasticsearch_cluster_name}"
  }
}
